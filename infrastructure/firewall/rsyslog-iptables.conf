# Capture iptables kernel log messages to a dedicated file.
# kern.warning is the facility/level used by iptables LOG target.
# No "& stop" so messages also keep flowing to /var/log/syslog (picked up by Wazuh).
kern.warning    /var/log/firewall/iptables.log
